import * as React from 'react';
import {Text, View} from 'react-native';

interface CreditCardPageViewProps {}

const CreditCardPageView = (props: CreditCardPageViewProps) => {
  return (
    <View>
      <Text>CreditCardPageView</Text>
    </View>
  );
};

export default CreditCardPageView;
