import {StyleSheet} from 'react-native';
import { colors } from '../../../theme/color';

const styles = StyleSheet.create({
  container: {
    borderBottomWidth: 1,
    borderBottomColor: '#E5E5E5',
    alignItems: 'center',
  },
  textInput: {fontSize: 24, color: colors.dark_blue, width: '100%'},
});
export default styles;
