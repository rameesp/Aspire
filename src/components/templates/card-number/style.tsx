import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    justifyContent: 'space-between',
    width: '70%',
  },
  priceView: {letterSpacing: 3},
});
export default styles;
